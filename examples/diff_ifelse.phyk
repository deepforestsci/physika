# Differentiable conditional

def f(x: ℝ): ℝ:
    if x > 0.0:
        return x * x
    else:
        return - x

# positive bracnh
a : ℝ = 3
f(a)
grad(f(a), a)

# negative branch
b : ℝ = - 2
f(b)
grad(f(b), b)
