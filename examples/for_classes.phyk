class SumArray(w : ℝ[n]):
    def λ(x : R[n]) → R:
        total : R = 0
        for i:
            total = total + x[i]
        return total

arr : ℝ[4] = [1.0, 2.0, 3.0, 4.0]
model = SumArray(arr)
model([1.0, 2.0, 3.0, 4.0])